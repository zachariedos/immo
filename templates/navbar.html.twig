<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="#">Symfo Immo</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('_profiler_home', relative = false) }}">Accueil</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="#">Link</a>
			</li>
		</ul>
		<ul class="navbar-nav ml-auto">
			{% if not app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('fos_user_security_login', relative = false) }}">Connexion</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('fos_user_registration_register', relative = false) }}">Inscription</a>
				</li>
			{% endif %}
			{% if app.user %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
						<i class="fa fa-user"></i>
						{{ app.user.username }}</a>
					<div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
						<a class="dropdown-item" href="{{ path('fos_user_profile_show', relative = false) }}">Profil</a>
						<a class="dropdown-item" href="{{ path('fos_user_change_password', relative = false) }}">Changer de Mot De Passe</a>
						<a class="dropdown-item" href="{{ path('fos_user_security_logout', relative = false) }}">Deconnexion</a>
					{% endif %}
				</li>
			</div>
		</div>
	</div>
</li></ul></div></nav>
