{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
	<p>{{ 'profile.show.username'|trans }}:
		{{ user.username }}</p>
	<p>{{ 'Nom' }}:
		{{ user.nom }}</p>
	<p>{{ 'Prénom' }}:
		{{ user.prenom }}</p>
	<p>{{ 'profile.show.email'|trans }}:
		{{ user.email }}</p>
	<a href="{{ path('fos_user_profile_edit', relative = false) }}">Modifier votre Profil</a>
</div>
